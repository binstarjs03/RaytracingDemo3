# RaytracingDemo

This is my personal experiment project learning on how raytracing works.

How to build:
- This project targets .NET 8 so make sure .NET 8 SDK is installed in your machine.
- Clone the repository, open cmd, type `dotnet build` in root repository directory